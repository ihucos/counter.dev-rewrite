"""
TODO Implement

test host:
    user a cannot see hosts of user b but its own host

test query:
    user cannot see data from a different user
    entries are properly filtered by host, data__lgte and date_lte
    if entries with the same metric and value exist, their count is summed

"""
