## Updated plan

Three parts, in order.

### 1. Remove the stale legacy backend templating
The Django backend still carries a dead server-rendered site (`services/backend/templates/`: `base.html`, `index.html`, `privacy.html`, `page.html`, `includes/navbar.html`, `includes/footer.html`) that nothing uses — the live site is the static frontend (which has its own navbar/footer components and `/pages/privacy.html`). Remove:
- `services/backend/templates/` (entire directory)
- `services/backend/counter/views.py` (only contains `index`/`privacy` render views)
- The "Pages" URL routes (`""` and `privacy`) and the `.views` import in `services/backend/counter/urls.py`
- The `DIRS: [BASE_DIR / "templates"]` entry in `counter/settings.py` TEMPLATES (keep the engine itself — Django admin still needs it)

No tests or e2e reference these pages (verified).

### 2. Document the frontend at /docs
Add `docs/frontend.md` matching the existing docs' style (dense prose + tables + gotchas like `architecture.md`): the static site layout (HTML pages, `components/` web components, `js/utils.js` helpers, Pelican blog/help pipeline, load.js), how pages bootstrap auth via `apiGetJSON("/me")`, the `<base-navbar>` behavior and its document-event bus, the sessionStorage username cache and why it's keyed the way it is, and the cross-subdomain cookie/apiUrl setup. Also update `docs/architecture.md` if it references the removed backend templates.

### 3. Fix the stale username after logout (the approved fix)
- `navbar.js`: `noUser()` resets state fully (hide `.has-user`, clear `.fill-username`); remove dead `swa=` guard; intercept "Sign out" clicks → fetch logout with credentials → remove `navbar-username-cache-*` from sessionStorage → redirect to `/welcome.html`. Backend `/logout` unchanged.
- E2E test: after sign out, username gone and guest links visible.
- Run backend tests + e2e suite.